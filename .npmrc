# Keep transitive deps out of the root node_modules so packages must declare
# what they actually use. Important for a repo that will be split up later.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
